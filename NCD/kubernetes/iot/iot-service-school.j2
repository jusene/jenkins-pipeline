apiVersion: extensions/v1beta1
kind: Deployment
metadata:
    name: iot-service-school
    namespace: iot-{{ ENV }}
spec:
    replicas: 1
    revisionHistoryLimit: 3
    selector:
      matchLabels:
        app: iot-service-school
        env: {{ ENV }}
        tier: backend
        release: k8s-release
    template:
      metadata:
        labels:
          app: iot-service-school
          env: {{ ENV }}
          tier: backend
          release: k8s-release
      spec:
          containers:
          - name: iot-service-school
            image: reg.ops.com/iot-{{ ENV }}/iot-service-school:{{ TIMESTAMP }}
            imagePullPolicy: IfNotPresent
            args: ["{{ ENV }}"]
            resources:
              requests:
                memory: "1024Mi"
                cpu: "500m"
              limits:
                memory: "2048Mi"
                cpu: "1000m"
            ports:
            - name: iot-school
              containerPort: 8805

---
apiVersion: v1
kind: Service
metadata:
  name: iot-school-service
  namespace: iot-{{ ENV }}
spec:
  selector:
    app: iot-service-school
    tier: backend
    env: {{ ENV }}
    release: k8s-release
  ports:
  - port: 8805
    targetPort: 8805

---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: iot-school
  namespace: iot-{{ ENV }}
spec:
  rules:
  - host: iot-service-school.{{ ENV }}.sysadmin.com
    http:
      paths:
      - path: /
        backend:
          serviceName: iot-school-service
          servicePort: 8805
