apiVersion: extensions/v1beta1
kind: Deployment
metadata:
    name: iot-service-user
    namespace: iot-{{ ENV }}
spec:
    replicas: 1
    revisionHistoryLimit: 3
    selector:
      matchLabels:
        app: iot-service-user
        tier: backend
        env: {{ ENV }}
        release: k8s-release
    template:
      metadata:
        labels:
          app: iot-service-user
          tier: backend
          env: {{ ENV }}
          release: k8s-release
      spec:
          containers:
          - name: iot-service-user
            image: reg.ops.com/iot-{{ ENV }}/iot-service-user:{{ TIMESTAMP }}
            imagePullPolicy: IfNotPresent
            args: ["{{ ENV }}"]
            resources:
              requests:
                memory: "1024Mi"
                cpu: "500m"
              limits:
                memory: "2048Mi"
                cpu: "1000m"
            ports:
            - name: iot-user
              containerPort: 8802
            volumeMounts:
            - name: nfs
              mountPath: "/ddhome/local/nginx/html/download"
          volumes: 
          - name: nfs
            nfs:
              server: 192.168.55.91
              path: "/ddhome/local/nginx/html/download"
              
---
apiVersion: v1
kind: Service
metadata:
  name: iot-user-service
  namespace: iot-{{ ENV }}
spec:
  selector:
    app: iot-service-user
    tier: backend
    env: {{ ENV }}
    release: k8s-release
  ports:
  - port: 8802
    targetPort: 8802

---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: iot-user
  namespace: iot-{{ ENV }}
spec:
  rules:
  - host: iot-service-user.{{ ENV }}.sysadmin.com
    http:
      paths:
      - path: /
        backend:
          serviceName: iot-user-service
          servicePort: 8802
