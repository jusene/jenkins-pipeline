apiVersion: extensions/v1beta1
kind: Deployment
metadata:
    name: ai-lab-service-config
    namespace: aicloud-{{ ENV }}
spec:
    {% if ENV == "prod" %}
    replicas: 1
    minReadySeconds: 60
    {% else %}
    replicas: 1
    {% endif %}
    revisionHistoryLimit: 3
    selector:
      matchLabels:
        app: ai-lab-service-config
        env: {{ ENV }}
        tier: backend
        release: k8s-release
    template:
      metadata:
        labels:
          app: ai-lab-service-config
          env: {{ ENV }}
          tier: backend
          release: k8s-release
      spec:
          {% if ENV == "prod" %}
          imagePullSecrets:
          - name: regsecret
          {% endif %}
          containers:
          - name: ai-lab-service-config
            {% if ENV == "prod" %}
            image: reg.k8s.com/aicloud-{{ ENV }}/ai-lab-service-config:{{ TIMESTAMP }}
            {% else %}
            image: reg.ops.com/aicloud-{{ ENV }}/ai-lab-service-config:{{ TIMESTAMP }}
            {% endif %}
            imagePullPolicy: IfNotPresent
            args: ["{{ ENV }}"]
            resources:
              requests:
                memory: "1024Mi"
                cpu: "500m"
              limits:
                memory: "2048Mi"
                cpu: "1000m"
            ports:
            - name: service-config
              containerPort: 8804
            

---
apiVersion: v1
kind: Service
metadata:
  name: ai-config-service
  namespace: aicloud-{{ ENV }}
spec:
  selector:
    app: ai-lab-service-config
    tier: backend
    env: {{ ENV }}
    release: k8s-release
  ports:
  - port: 8804
    targetPort: 8804

---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ai-lab-service-config
  namespace: aicloud-{{ ENV }}
 
spec:

  rules:
  - host: ai-lab-service-config.{{ ENV }}.sysadmin.com
    http:
      paths:
      - path: /
        backend:
          serviceName: ai-config-service
          servicePort: 8804


